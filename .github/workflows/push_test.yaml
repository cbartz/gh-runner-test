name: Push Event Tests

on:
  push:

jobs:
  push-event-tests:
    runs-on: [self-hosted]
    steps:
      - name: hello world
        run: echo "Hello, world"
      - name: provoke a oom by allocating a lot of memory
        run: dd if=/dev/zero bs=1G count=16 2>/dev/null