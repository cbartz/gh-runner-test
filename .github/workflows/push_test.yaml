name: Push Event Tests

on:
  push:

jobs:
  push-event-tests:
    runs-on: [self-hosted]
    steps:
      - name: hello world
        run: echo "Hello, world"
      - name: sleep 5 minutes
        run: sleep 300