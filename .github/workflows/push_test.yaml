name: Push Event Tests

on:
  push:

jobs:
  push-event-tests:
    runs-on: [self-hosted]
    steps:
      - name: hello world
        run: echo "Hello, world"
      - name: provoke a oom by allocating a lot of memory
        run: |
         python3 -c "[x for x in xrange(2**30)]"
